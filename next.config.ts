import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev-tools indicator (dev only; never in prod).
  devIndicators: false,
};

export default nextConfig;
